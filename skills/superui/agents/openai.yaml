interface:
  display_name: "SuperUI"
  short_description: "Use when users mention SuperUI, $superui, or frontend UI work"
  default_prompt: "Use $superui when the user explicitly mentions SuperUI/$superui/this skill, or to build, redesign, analyze, implement, or review frontend UI with durable design artifacts."

policy:
  allow_implicit_invocation: true
